<%= form_with(model: [@article, @article.comments.build], local: true) do |form| %>
  <p>
    <%= form.text_field :commenter, placeholder: "Name" %>
  </p>
  <p>
    <%= form.text_area :body, placeholder: "Text" %>
  </p>
  <p>
    <%= form.submit %>
  </p>
<% end %>